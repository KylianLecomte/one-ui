import { GetCellValuePipe } from './get-cell-value.pipe';

describe('GetCellValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCellValuePipe();
    expect(pipe).toBeTruthy();
  });
});
